# Evidence images — onyx ticket 86bbge60h

Public host for the screenshots embedded in the pull request that fixes the silently-dropped
firewall session write. Images only; captured on a local Kubernetes dev cluster against seeded
synthetic data. See the PR body for what each one proves.
